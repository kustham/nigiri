import messageStyle from '@/styles/components/message.module.scss'

const MessagePanel = () => {
    return (
        <section className={messageStyle.formSection}>
            <div>
                <p>message</p>
            </div>
        </section>
    )
}

export default MessagePanel
